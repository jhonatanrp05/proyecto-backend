# Application
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sqlplatform"

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_SECRET=dev-secret
JWT_EXPIRATION=1d

# Google Gemini IA (Plan Gratuito)
GEMINI_API_KEY=tu_api_key_de_gemini_aqui
